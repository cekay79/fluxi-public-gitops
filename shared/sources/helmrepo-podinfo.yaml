apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: podinfo
  namespace: flux-system     # shared location
spec:
  interval: 10m
  url: https://stefanprodan.github.io/podinfo
