apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: petclinic
  namespace: flux-system
spec:
  chart:
    spec:
      chart: petclinic
      version: "0.1.0-3c2c309"
      sourceRef:
        kind: HelmRepository
        name: ckcluster
        namespace: flux-system
  values:
    replicaCount: 1